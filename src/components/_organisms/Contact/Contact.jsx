import styles from "./Contact.module.scss";

const Contact = (props) => {
  return (
    <div className={styles["container"]}>
        <h2 className={styles["title"]}>4. Contact Me</h2>
    </div>
  );
};
export default Contact;
